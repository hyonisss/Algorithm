import java.util.*;
class Solution {
    public int solution(int bridge_length, int weight, int[] truck_weights) {
        Queue<Integer> queue = new LinkedList<Integer> ();
        int answer = 0;
        for(int truck: truck_weights) {
            for(int i=0; i<bridge_length; i++) {
                if(queue.)
            }
            while(queue.size()<=length) 
            
            
            if(queue.isEmpty()) { //다리를 건너는 트럭이 없으면 트럭 집어넣기
                queue.offer(truck);
            }
            else {        
                
                //다리를 건너는 트럭이 있으면 다음 실행
                while(!queue.isEmpty()) { //트럭 무게 고려 조건문
                    int cur_weight += queue.peek(); //다리 위의 트럭 무게 다 더하기
                    int to_weight = truck + cur_weight; //새로 들어갈 트럭 무게까지 더하기
                     if(to_weight<=weight) {//그 무게의 합이 다리가 견디는 무게보다 작거나 같으면
                         queue.offer(truck); //현재 트럭 집어 넣기
                    }
                    
                }
            }          
        }      
        return answer;
    }
}