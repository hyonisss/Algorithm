1.	홀수의 개수
[문제 설명] 1개의 양의 정수 n을 입력 받아 1부터 n까지 홀수의 개수를 인쇄한다.
[입력 예시] 20
[출력 예시] 10

#include<stdio.h>
void main(void)
{
	int a, cnt =0;
	scanf_s("%d", &a);
	for (int i = 1; i <= a; i += 2) {
		cnt++;
	}
	printf("%d", cnt);
}
2.	홀수의 합계
[문제 설명] 1개의 양의 정수 n을 입력 받아 1부터 n까지 홀수의 합계를 인쇄한다.
[입력 예시] 20
[출력 예시] 100 
#include<stdio.h>
void main(void)
{
	int a;
	long long int sum = 0;
	scanf_s("%d", &a);
	for (int i = 1; i <= a; i += 2) {
		if(i%2) sum += i;
	}
	printf("%lld", sum);
}
* Type 범위 잘 보고 타입변수 사용하기 
